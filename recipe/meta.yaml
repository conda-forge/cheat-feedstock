{% set name = "cheat" %}
{% set version = "2.3.1" %}
{% set sha256 = "fd26d012e5057207cd79c63f81b99b647fb3a099f02680eb4c7d8894ba655189" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python

  run:
    - python
    - docopt >=0.6.1
    - pygments >=1.6.0

test:
  imports:
    - cheat
    - cheat.cheatsheets
    - cheat.test

about:
  home: https://github.com/chrisallenlane/cheat
  license: MIT/GPL3
  license_file: LICENSE
  summary: 'cheat allows you to create and view interactive cheatsheets on the command-line. It was designed to help remind *nix system administrators of options for commands that they use frequently, but not frequently enough to remember.'
  dev_url: https://github.com/chrisallenlane/cheat

extra:
  recipe-maintainers:
    - proinsias
